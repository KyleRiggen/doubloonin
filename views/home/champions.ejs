<%- include('../includes/head.ejs') %>
</head>

<body>
    <%- include('../includes/nav.ejs') %>

    <main>
        <% if (champs.length > 0) { %>
            <% for (let champ of champs) { %>
                <p><%= champ.points %> | <%= champ.name %></p>
                <p>image url: <%= champ.picture %></p>
            <% } %>
        <% } else { %>
            <h1>no champions found. champions file not greater than 0</h1>
        <% } %>
    </main>
    <%- include('../includes/end.ejs') %>